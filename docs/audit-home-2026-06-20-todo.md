# Audit TODO — home section + shared infra

Generated from [audit-home-2026-06-20.md](./audit-home-2026-06-20.md). Check off as you go.

## P0 — safe, zero-risk (do first, batch these)  ✅ DONE 2026-06-20 (parallel swarm)

- [x] Delete `components/ui/card.tsx` (entire file dead, 7 exports unused)
- [x] Delete `components/ui/badge.tsx` (`Badge` + `badgeVariants` dead)
- [x] Remove dead CSS: `.link-underline-active::after` at `app/globals.css:167`
- [x] Remove dead CSS: `.side-note` at `app/globals.css:270`
- [x] ~~Add `.env.preview` to `.gitignore`~~ — **already present** (`.gitignore:42`; `git check-ignore .env.preview` confirms it's ignored). Audit was stale; no change needed.
- [x] Drop `export` from 7 internal-only symbols in `lib/github.ts` (lines 132, 138–141, 150, 160)
- [x] Drop `"use client"` from `components/ui/label.tsx` (no hooks) → Server Component
- [x] Drop `"use client"` from `components/sections/education.tsx` (uses only already-client subcomponents) → Server Component
- [x] Drop `"use client"` from `components/sections/contact-cta.tsx` (uses only already-client subcomponents) → Server Component

## P1 — high-impact behavior/perf fixes  ✅ DONE 2026-06-20 (parallel swarm; `tsc` clean, `next build` green, `/` static ○ Revalidate 6h)

- [x] **Fix `error.tsx` navbar loss:** rendered `<Navbar />` inside `error.tsx` (matching `not-found.tsx`) and corrected the false comment. Chose the minimal option to keep the parallel agents' file-ownership disjoint (the `layout.tsx` hoist was the alternative and is still open if you want navbar to survive global errors too).
- [x] **Move GitHub data to server-side props:** `app/page.tsx` is now `async`, awaits `getGitHubActivity()`, passes `initialData` to the lazy GitHubActivity wrapper; `app/api/github/route.ts` stays as the refetch endpoint (client no longer fetches on mount when `initialData` is present); added `app/loading.tsx`. A "Refresh" button was added to expose the refetch path. Trade-off: await-at-top gates the whole page on a cold cache (`loading.tsx` shows meanwhile); on the warm 6h cache the await is near-instant. (A per-section `<Suspense>` around the GitHub fetch would let Hero paint first on cold cache — optional refinement.)
- [x] **Code-split below-fold sections:** new `components/lazy-sections.tsx` (client `next/dynamic` + `useNearViewport` IntersectionObserver) lazy-loads the **4 client sections** (GitHubActivity, Experience, Skills, Certifications). Per your decision, Education & ContactCta are **Server Components** (P0 #8/#9) and render eagerly — Server Components can't be client-lazy-loaded (Next.js 16 lazy-loading doc), so they're not wrapped in LazySection.
- [x] **Lazy-load heavy global components:** new `components/client-globals.tsx` (client wrapper) dynamically imports `command-palette.tsx` + `spotlight.tsx` with `ssr:false`; mounts Spotlight on first pointermove, CommandPalette on first ⌘K / `open-command-palette` event (re-dispatches open after chunk load so the first interaction opens it). `app/layout.tsx` now renders `<ClientGlobals/>`.
- [x] **`hero.tsx` `LocalTime`:** imperative `ref.current.textContent` update in the interval — no per-second re-render. Hydration-safe (empty span on server + first client render, filled via effect).
- [x] **`spotlight.tsx`:** `mousemove` writes `--spotlight-x/y` CSS vars via ref + rAF — no React re-render per move. Also **added** `prefers-reduced-motion` gating (the file previously ignored it; returns null under reduced motion), matching the codebase's consistent reduced-motion discipline.
- [x] **`code-particles.tsx`:** the audit premise didn't fully hold — `ctx.font` varies per particle (built from per-particle `size`, 9–11px), so a single hoist would change rendering. Instead the font string is precomputed **once at particle creation** (`p.fontStr`) and reused in the loop — eliminates ~55 string allocations/frame with identical output. Count kept at 55.

## P2 — security / a11y / SEO (launch-readiness)  ✅ DONE 2026-06-22 (parallel swarm; `tsc` clean, `next build` green, `/` static ○ Revalidate 6h)

- [x] Add a **honeypot** field to `contact-form.tsx`; reject non-empty server-side in `app/api/contact/route.ts`.
- [x] Add **skip-to-content link** as first child of `<body>` in `app/layout.tsx`; add `id="main"` to `<main>`. — skip link is first child of `<body>`; `id="main"` lives on a focusable `className="contents"` wrapper around `{children}` in `layout.tsx` (the `<main>` element is in `app/page.tsx`, owned separately, so the permitted "top content wrapper" fallback was used).
- [x] Add **print styles** for the resume page (`@media print` in `app/globals.css` — hide navbar/footer, expand layout).
- [x] Wire up **minimal observability**: `@vercel/analytics` + `@vercel/speed-insights`, and/or Sentry; forward `error.tsx`/`global-error.tsx` digests to a server endpoint. — new `app/api/error/route.ts` (POST, 204) receives the digest; `error.tsx` + `global-error.tsx` fire-and-forget POST it on mount. Vercel scripts are same-origin (`/_vercel/...`), so no CSP change was needed.
- [x] Add **JSON-LD `Person`** schema on home; `BreadcrumbList` on project pages.
- [x] Add **per-page `canonical`** to each `metadata` export.

## P3 — cleanups / polish  ✅ DONE 2026-06-22 (parallel swarm; `tsc` clean, `next build` green)

- [x] Extract shared `listContainer`/`listItem` Framer variants + the `[0.25,0.1,0.25,1]` easing into `lib/motion.ts` (15+ inline sites across 5 sections). — `lib/motion.ts` exports `EASE`, `listContainer`, `listItem`, and a `makeListContainer(stagger, delayChildren?)` factory. `skills`/`certifications`/`featured-projects` now import from it (skills keeps its exact 0.08/0.15 timing via the factory). `github-activity` keeps its variants local in `github-activity/shared.ts` (it was split in the same pass by a different owner); unifying those onto `lib/motion.ts` is an optional follow-up.
- [x] Reconcile `components/ui`: delete unused exports OR adopt primitives in sections (decision resolves both dead-code + architecture findings). — went with "delete unused exports": `buttonVariants` un-exported, 4 variants + 5 sizes removed; dialog trimmed to `Dialog`/`DialogContent`/`DialogTitle` (portal/overlay kept internal).
- [x] Trim `button.tsx` dead `buttonVariants` export + 9 unused CVA variant/size values; trim `dialog.tsx` to live exports (`Dialog`, `DialogContent`, `DialogTitle`).
- [x] Define explicit domain types in `lib/data.ts` (`Project`, `Certification`, `Education`, `SkillSet`) — mirror existing `Experience` interface; make optional fields intentional. — `SkillSet` carries an additive `[key: string]: string[]` index signature so `Object.entries(skills)` in `app/resume/page.tsx` stays typed.
- [x] `section-number.tsx:9-16` — `whileInView` `once:false` re-animates on every scroll; set `once:true`.
- [x] `github-activity.tsx:610-629` — ~365 per-cell `motion.div` each register viewport observers; animate container once via CSS `animation-delay` cascade. — `ContributionGraph` renders one `<style>` keyframe + per-cell inline `animationDelay`; `prefers-reduced-motion` renders cells static.
- [x] `github-activity.tsx:463-472` — replace raw `<img>` avatar with `next/image` + `remotePatterns` entry (CSP already allows the host).
- [x] `globals.css:280-291` — `.animate-conveyor` runs off-screen; toggle `animation-play-state` via IntersectionObserver or add `content-visibility:auto`. — chose the CSS-only `content-visibility:auto` + `contain-intrinsic-size` option (stays in owned files, no extra client component).
- [x] Split `github-activity.tsx` (926 lines) into `components/sections/github-activity/*` siblings (`ContributionGraph`, `ActivityRhythm`, `PinnedRepos`, `IdentityStrip`). — composer `github-activity.tsx` keeps the named `GitHubActivity` export + `{ initialData }` contract; "top languages" stayed in the composer (shares the grid row with `ActivityRhythm`).

## Verify (needs a production build / dev run)  ✅ DONE 2026-06-24 (`next build` green — Turbopack, TS clean, exit 0; OG visual check skipped per owner's model constraint — verified code-level instead)

- [x] **`next build` + chunk gzip sizes:** Turbopack build succeeded (exit 0, TS clean). `/` initial **executing** set = 15 chunks (extracted from the prerendered `index.html` `<script src=…>` tags, NOT prefetch `<link>`s) = **281 KiB gzipped** (287,805 B) + 1 CSS. Identified chunks: `0vr6eflop8zax` 72.5KB + `0k16r5kb_-ryo` 8.98KB + `1ckhybdnqav1-` 0.27KB (the 3 `rootMainFiles` = Next/React core runtime, per `build-manifest.json`), `0cz1d0mv5g_q7` 39.5KB (the `polyfillFiles` entry), `1tu1qk2_6558h` 44.2KB (framer-motion shared chunk, telltale-confirmed), `turbopack-208x3tu57h87d` 4.2KB (Turbopack on-demand chunk-loader runtime). All 29 client chunks total 460 KiB gzipped; `/` loads 15. ⚠️ Note: 281 KiB exceeds the 150 KiB landing-page budget — framer-motion + above-the-fold motion sections dominate; the lazy-splitting (next item) is what keeps it from being larger.
- [x] **Bundle-analyzer / no-leak:** `@next/bundle-analyzer` is **not installed** and is webpack-only (Next 16 builds on Turbopack by default), so verified by **direct chunk inspection**. **framer-motion** = shared chunk `1tu1qk2_6558h.js` ≈ **44 KiB gzipped** (dominant non-runtime vendor cost in `/`'s initial set). **react-email / resend / @upstash/redis:** precise SDK-telltale scan (`api.resend.com`, `RESEND_API_KEY`, `new Resend`, `@resend`, `react-email`, `@react-email`, `upstash`, `@upstash/redis`, `Redis.fromUrl`) across **all** `.next/static/chunks/*.js` = **EMPTY** → none leak to the client. Source confirms they're server-only: `app/api/contact/route.ts:3-5` imports `Resend`/`Redis`/`render`; `emails/*` use `react-email`. (A loose `re[sS]end` match on the Vercel-Analytics chunk `3hrycviv18-se.js` was the `beforeSend` substring — false positive, debunked via surrounding-context grep.)
- [x] **`/` prerenders static:** route table shows `○ /` Static, Revalidate 6h, Expire 1y. `prerender-manifest.json` lists `/` with `initialRevalidateSeconds: 21600`, `initialExpireSeconds: 31536000`, `dataRoute: /index.rsc`. ✓ (Σ static.)
- [x] **`next/dynamic` shrinks `/` initial chunk (Turbopack v16):** the 15-chunk `/` executing set **excludes** Spotlight (`3-dmjnth4gzbb.js`), CommandPalette (`2er29mgi86fk5.js`, 19.9KB — located via the unique `command-palette-listbox` telltale), Skills (`37emom1dtxlbr.js`), GitHub graph (`1ox71-n3o7w7-.js`, 11.9KB), the Certifications chunks (`001n7c8zcbzy7.js`/`2121ekyhr-sgp.js`/…), and the single largest client chunk `1j_npoz7r4f84.js` (80.6KB) — all deferred. "Technical Skills" is absent from every executing chunk (lives only in the deferred palette+skills chunks). The Turbopack on-demand loader runtime (`turbopack-208x3tu57h87d.js`, 4.2KB) **is** in the executing set. Experience uses the identical `LazyBoundary`+`dynamic(ssr:false)` wrapper (deferred by construction). So the wrapper keeps Spotlight + CommandPalette + the 4 lazy sections out of `/`'s initial chunk under Turbopack v16. ✓
- [x] **OG font — Geist (code-level; visual check skipped):** `app/api/og/route.tsx:38` sets `fontFamily: "Geist Sans, sans-serif"` but the `ImageResponse` options (lines 114-122) pass **no `fonts` array**. Per the bundled `image-response.md`, a custom family must be registered via `fonts: [{name, data: ArrayBuffer, weight, style}]`; without it, "Geist Sans" matches nothing and `next/og` falls back to the `sans-serif` default. **Conclusion: the OG image does NOT render Geist — system sans fallback.** Defect confirmed (matches audit-home-2026-06-20.md:79). The OG route itself is healthy live (200 `image/png`, ~12KB, immutable cache). **Fix NOT done (beyond verify scope):** register Geist via `fonts`; needs a ttf/otf/woff Geist file (woff2 is unsupported) and either (a) `runtime: "nodejs"` + `readFile(join(process.cwd(),…))` per the doc example, or (b) keep edge + runtime `fetch()` of the font bytes. Left for owner decision.
- [x] **`metadataBase` matches deployed domain:** `metadataBase = new URL("https://daniel-est.vercel.app")` (layout.tsx:61); home canonical `/` (page.tsx:21) resolves to `https://daniel-est.vercel.app/`. Live probe: `GET https://daniel-est.vercel.app/` → **200**, no redirect (canonical host), returned CSP exactly matches `next.config.ts` → deployed build == this code. OG footer (og/route.tsx:110) and JSON-LD `url`/`image` (page.tsx:30-31) agree. ✓